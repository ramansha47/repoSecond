def sum(arr):
    sum = 0
    for i in range(0, len(arr)):
        for j in range(0, len(arr2)):
            sum = sum + (arr[i]*arr2[j]);    
    return sum
# Test array
arr = [ 2, 3, 4, 10, 40 ]
arr2 = [10, 20, 2, 50, 20, 15]
result = sum(arr)

print(result)
